<h1><%= @page.title %></h1>
<p>Posted on <%= Timex.format!(@page.date, "{YYYY}-{0M}-{0D}") %> by <%= @project.authors[:john][:name] %></p>
<%= unless Enum.empty?(@page.tags) do %>
  <p>Tags:</p>
  <ul>
    <%= for t <- @page.tags do %>
      <li><a href="<%= t.url %>"><%= t.name %></a></li>
    <% end %>
  </ul>
<% end %>
<%= @contents %>
